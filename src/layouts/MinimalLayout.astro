---
/**
 * Minimal Layout
 *
 * Full-width layout without header or footer.
 * Ideal for standalone pages like 404, maintenance, or landing pages
 * that need complete control over the viewport.
 *
 * @example
 * ```astro
 * <MinimalLayout title="Page Not Found">
 *   <div class="flex items-center justify-center min-h-screen">
 *     <h1>404</h1>
 *   </div>
 * </MinimalLayout>
 * ```
 */
import "@/styles/globals.css";
import SEO from "@/components/SEO.astro";
import FontPreload from "@/components/FontPreload.astro";
import ThemeScript from "@/components/ThemeScript.astro";

interface Props {
  title: string;
  description?: string;
  image?: string;
  noindex?: boolean;
}

const { 
  title, 
  description = "Telzee gives small businesses a real business number, smart call routing, voicemail transcription, SMS inbox, and clear monthly pricing.",
  image,
  noindex = false,
} = Astro.props;

const fullTitle = `${title} - Telzee`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <FontPreload />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="/favicon-192.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <title>{fullTitle}</title>
    <SEO 
      title={fullTitle}
      description={description}
      image={image}
      noindex={noindex}
    />
    <ThemeScript />
  </head>
  <body class="min-h-screen antialiased">
    <main id="main-content" class="min-h-screen">
      <slot />
    </main>
  </body>
</html>
